server:
  port: 8030
spring:
  application:
    name: client
  thymeleaf:
    profix: classpath:/templates/
    suffix: .html
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
